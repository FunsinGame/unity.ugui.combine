## 项目概述
使 UGUI 像 UI Toolkit 一样尽可能以单次 Draw Call 绘制整个 UI 界面。

## 核心特性
- 兼容所有 UGUI 组件及 TMP_UI 组件。
- 合并开关可实时启用/关闭，支持按 Canvas 或组件粒度设置。
- 支持导出内置 Sprite，组件默认sprite引用外部图集。
- TMP 当前仅支持 TMP_SDF-Mobile.shader 。
- 兼容 RectMask2D 合并。
  
## 未来规划
- 支持更完整的 TMP 参数。
